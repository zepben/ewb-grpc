name: Meter
description: Physical asset that performs the metering role of the usage point. Used for measuring consumption and detection
  of events.
attributes:
- name: companyMeterId
  type: String
  nullable: true
  description: The meter ID managed by the company that owns this meter.
ancestors:
- EndDevice