name: DependencyKind
description: Enum describing the different relationships two objects may have.
attributes:
  - name: mutuallyExclusive
    description: The dependencies cannot exist together.
  - name: required
    description: The dependencies must exist together.
