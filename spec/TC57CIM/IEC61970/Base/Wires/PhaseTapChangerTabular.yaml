name: PhaseTapChangerTabular
description: Describes a tap changer with a table defining the relation between the tap step and the phase angle difference
  across the transformer.
ancestors:
- PhaseTapChanger
associations:
- source: PhaseTapChangerTabular
  target: PhaseTapChangerTable
  sourceCardinality: 0..*
  sourceName: 0..1
  sourceDescription: PhaseTapChangerTabular
  targetCardinality: The phase tap changers to which this phase tap table applies.
  targetName: PhaseTapChangerTable
  targetDescription: The phase tap changer table for this phase tap changer.