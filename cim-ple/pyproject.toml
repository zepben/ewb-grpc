[project]
name = "cim-ple"
version = "0.1.0"
description = "Terminal CIM specification browser"
requires-python = ">=3.10"
dependencies = [
    "PyYAML>=6.0",
    "ruamel.yaml>=0.18",
    "textual>=0.47",
]

[project.scripts]
cim-ple = "cim_ple.main:main"

[tool.ruff]
line-length = 120
